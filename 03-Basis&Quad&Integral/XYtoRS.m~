function [r,s] = XYtoRS(x_list,y_list,Jk,vertice_list)
    
    [~,Inv_AffineMap] = Ref_Tri_Map(Jk,vertice_list);
    V1 = vertice_list(1,:)';
    
    pts_v = [x_list';y_list'] - V1;
    
    temp = Inv_AffineMap*r_v + V1;
    
end