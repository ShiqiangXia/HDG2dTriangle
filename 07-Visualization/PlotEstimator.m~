function PlotEstimator(mymesh,estimator)
    
    p = mymesh.vertices_list;
    e = mymesh.element_list;
    
    myfigure = trisurf(e,p(:,1),p(:,2),0*p(:,0));
    temp = abs(estimator);
    max_err = max(temp)
    
    color_list = temp/max_err;
    
    
end